services:
  - type: web
    name: finviz-agent-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn finviz_api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: PYTHONPATH
        value: .
